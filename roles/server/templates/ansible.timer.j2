[Unit]
Description=Daily ansible configuration activities

[Timer]
OnCalendar=*-*-* {{ on_calendar_ansible | default("6:00") }}
RandomizedDelaySec={{ randomized_delay_ansible | default("60m") }}
Persistent=true

[Install]
WantedBy=timers.target
