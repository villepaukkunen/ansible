[Unit]
Description=Daily ansible configuration activities

[Timer]
OnCalendar=*-*-* 6:00
RandomizedDelaySec={{ randomized_delay_ansible | default("30m") }}
Persistent=true

[Install]
WantedBy=timers.target
